<%= f.input :buscaso_ids,
  collection: ['Todos', 'Si', 'No'],
  label: false,
  selected: Msip::ModeloHelper.poromision(params, :buscaso_ids),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => '',
    value: params && params[:filtro] && params[:filtro][:caso_ids] ? 
      params[:filtro][:caso_ids] : 'Todos'
  },
  include_blank: false
%>
