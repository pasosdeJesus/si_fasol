<% nacionalde_id = params && params[:filtro] && 
  params[:filtro][:busnacionalde] ? 
  params[:filtro][:busnacionalde].to_i : 0 %>
<%= f.input :busnacionalde,
  collection: Msip::Pais.habilitados,
  label: false,
  label_method: :nombre,
  valud_method: :id,
  selected: Msip::ModeloHelper.poromision(params, :busnacionalde),
  input_html: {
    class: 'tom-select',
    data: {
      "enviarautomatico": "",
    },
    value: nacionalde_id
  },
  include_blank: true
%>
