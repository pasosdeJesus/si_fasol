<%= f.input :busfamiliarvictima_ids,
  collection: ['Todos', 'Si', 'No'],
  label: false,
  selected: Msip::ModeloHelper.poromision(params, :busfamiliarvictima_ids),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => '',
    value: params && params[:filtro] && params[:filtro][:familiarvictima_ids] ? 
      params[:filtro][:familiarvictima_ids] : 'Todos'
  },
  include_blank: false
%>
